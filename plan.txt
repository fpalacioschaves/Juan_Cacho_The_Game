Ritmo de trabajo por capítulo

Guion jugable (objetivos, flags, 1–2 puzzles, 2–3 escenas).

Implementación (UI + lógica + assets mínimos).

Pulido (ramas extra, líneas nuevas, hints escalonados).

QA breve y a la pila del siguiente.

Backlog sistémico (lo metemos cuando toque pulir):

Diálogos ramificados con condiciones (nodos + flags).

Hints “de escalera” y libretita más útil (marca pistas pendientes).

Animaciones suaves y feedback táctil/móvil.

Ambient SFX por localización (bar, calle, academia).

Accesibilidad: foco/teclado, descripciones ARIA en hotspots.

Refactor a módulos (state, ui, puzzles), sin romper lo hecho.

Localización completa (UI estática) + migrador de saves.